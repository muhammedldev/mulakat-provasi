import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.muhammedldev.mulakatprovasi',
  appName: 'Mülakat Provası',
  webDir: 'dist'
};

export default config;
